#!/usr/bin/env python3

class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        prevMap = {}
        
        for i in enumerate(nums):
            diffre = target - n 
            if diffre in prevMap:
                return 
            